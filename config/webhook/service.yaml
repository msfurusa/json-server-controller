apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: system
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: json-server-controller
    app.kubernetes.io/managed-by: kustomize
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
    name: webhook-server
  selector:
    control-plane: controller-manager
